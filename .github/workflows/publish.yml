name: Release to npm

on:
  push:
    tags: ["v*"]

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: oven-sh/setup-bun@v1
        with:
          bun-version: latest
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          registry-url: https://registry.npmjs.org
      - run: bun install --frozen-lockfile
        working-directory: app
      - run: npx tsc --noEmit
        working-directory: app
      - name: Publish justtuit (unscoped - owned by webboxescom)
        run: npm publish --access public
        working-directory: app
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPMTOKEN_WEBBOXES }}
      - name: Publish @webboxes/justtuit alias (org - musichen is a member)
        run: npm publish --access public
        working-directory: app/packages/webboxes-justtuit
        continue-on-error: true
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPMTOKEN_MUSICHEN }}
